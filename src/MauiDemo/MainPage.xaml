<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiDemo.MainPage">

    <Grid RowDefinitions="*,Auto">
        <CollectionView x:Name="EmployeesList" ItemsSource="{Binding Employees}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="10" Spacing="5">
                        <Label Text="{Binding Name}" />
                        <Label Text="{Binding Position}" />
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <HorizontalStackLayout Spacing="10" Padding="10" HorizontalOptions="Center" Grid.Row="1">
            <Button Text="Add Range (5)" Command="{Binding AddRangeCommand}" />
            <Button Text="Clear" Command="{Binding ClearItemsCommand}" />
        </HorizontalStackLayout>
    </Grid>
</ContentPage>
